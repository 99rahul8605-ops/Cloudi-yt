services:
  - type: web
    name: yt-downloader-bot
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: BOT_TOKEN
        sync: false          # Set this manually in Render dashboard
      - key: PORT
        value: "8080"
    healthCheckPath: /
    autoDeploy: true
